
# Reproducibility Guide

This document explains how to run the public demo workflow of this repository.

The original clinical data are not included. The public workflow is designed to run on synthetic demo data or on authorized local data with the same expected structure.

---

## 1. Install Dependencies

Install the required Python packages:

```bash
pip install -r requirements.txt
